import React from 'react';
import ReactDOM from 'react-dom';
import Board from './components/Board';

const containerElement = document.getElementById('container');
ReactDOM.render(<Board />, containerElement);
